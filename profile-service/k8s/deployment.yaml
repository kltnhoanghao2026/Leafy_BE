apiVersion: apps/v1
kind: Deployment
metadata:
  name: leafy-profile-service
  namespace: leafy
spec:
  replicas: 1
  selector:
    matchLabels:
      app: profile-service
  template:
    metadata:
      labels:
        app: profile-service
    spec:
      nodeSelector:
        node-type: app
      initContainers:
        - name: wait-for-config-server
          image: busybox
          command:
            - sh
            - -c
            - |
              until wget -qO- http://leafy-config-server:8888/actuator/health/readiness 2>/dev/null | grep -q '"status":"UP"'; do
                echo "Waiting for config-server..."
                sleep 5
              done
              echo "Config server is UP"
      containers:
        - name: profile-service
          image: 383704034208.dkr.ecr.ap-southeast-1.amazonaws.com/leafy/profile-service:latest
          imagePullPolicy: Always
          env:
            - name: SPRING_CLOUD_CONFIG_URI
              valueFrom:
                configMapKeyRef:
                  name: leafy-common-config
                  key: SPRING_CLOUD_CONFIG_URI
            - name: MONGODB_DATABASE_PROFILE
              valueFrom:
                secretKeyRef:
                  name: leafy-db-secrets
                  key: MONGODB_DATABASE_PROFILE
          envFrom:
            - configMapRef:
                name: leafy-common-config
            - secretRef:
                name: leafy-common-secrets
          ports:
            - containerPort: 8087
              name: http
          resources:
            requests:
              memory: 512Mi
              cpu: 300m
            limits:
              memory: 1536Mi
              cpu: 1000m
          readinessProbe:
            httpGet:
              path: /actuator/health/readiness
              port: 8087
            initialDelaySeconds: 30
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 3
