spring:
  application:
    name: profile-service
  config:
    import: optional:configserver:http://localhost:8888
  cloud:
    config:
      enabled: false

server:
  port: ${SERVER_PORT_PROFILE:8083}
