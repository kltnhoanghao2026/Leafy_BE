apiVersion: v1
kind: Service
metadata:
  name: leafy-postgresql
  namespace: leafy
spec:
  ports:
    - port: 5432
      targetPort: 5432
      name: postgresql
  selector:
    app: postgresql
  clusterIP: None
