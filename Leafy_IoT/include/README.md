# Include Directory

Place shared public headers here only when they need to be included outside `src/`.

The current scaffold keeps module headers next to their implementations under `src/app`, `src/models`, and `src/utils`.
