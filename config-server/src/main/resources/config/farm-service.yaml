server:
  port: ${SERVER_PORT_FARM}

spring:
  data:
    mongodb:
      host: ${MONGODB_HOST}
      port: ${MONGODB_PORT}
      database: ${MONGODB_DATABASE_FARM}
      username: ${MONGODB_USERNAME}
      password: ${MONGODB_PASSWORD}
      authentication-database: admin

farm:
  seeder:
    plots-per-profile: 2
    zones-per-plot: 3
    random-seed: 20260324
    profile-page-size: 200
    profile-max-pages: 3
